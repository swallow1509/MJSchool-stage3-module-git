1) Add a line to a file and commit changes
2) Add a line to a file and commit changes
3) Add a line to a file and commit changes
4) Add a line to a file and commit changes
5) Add a line to a file and commit changes
testing... final
